# Questarr Environment Configuration Template -- Used for NPM run (not for docker)

# --- Database Configuration ---
# You can provide a full connection string or individual components.
# If both are provided, DATABASE_URL takes precedence for the application.

# Individual components (used by docker-compose for the DB container)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=questarr
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Application connection string for backward compatibility. No need to change that if you're using Questarr for the first time.
# For Docker, use: postgresql://postgres:password@db:5432/questarr
# For local, use: postgresql://postgres:password@localhost:5432/questarr
# DATABASE_URL=postgresql://postgres:password@localhost:5432/questarr

# --- Authentication ---
# Recommended: Secret key for signing JWT tokens.
# If not provided, a secure random secret will be generated and stored in the database.
# Set this to a long random string to keep sessions valid across server restarts.
JWT_SECRET=change-this-to-a-secure-random-string

# --- IGDB API Configuration ---
# Required for game discovery and metadata features.
# Get credentials by registering an app at https://dev.twitch.tv/console
IGDB_CLIENT_ID=your_igdb_client_id
IGDB_CLIENT_SECRET=your_igdb_client_secret

# --- Server Configuration ---
# Optional: Port to run the server on (default: 5000)
PORT=5000
# Optional: Host to bind the server to (default: 0.0.0.0)
HOST=0.0.0.0
# Optional: Node environment (development/production/test)
NODE_ENV=production

name: Docker Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  # Allow manual trigger
  workflow_dispatch:

jobs:
  docker-build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Build Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: false
          tags: gameradarr:test
          cache-from: type=gha
          cache-to: type=gha,mode=max
          load: true
      
      - name: Verify Docker image
        run: |
          echo "Docker image built successfully"
          docker images | grep gameradarr || echo "Image not found in docker images, but build completed"
